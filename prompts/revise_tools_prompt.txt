First invoke the `experiment-background` skill and read its contents carefully. Then proceed with the following task.

Work through the newly created Python tools under `tools/`, which were created to support the `PLAN.md` and correspond to the experimental system described in the background above.

Based on the experimental system details and the PLAN.md at hand, please fix any obvious errors with any of the Python tools. For example: major physics errors.

Check if there are any FIT NOTES in the `experiment-background` skill and be sure the tools implement these notes. Restructure the code if not.

IMPORTANT: Make sure the computational methods are optimized with MULTIPROCCESSING and VECTORIZATION wherever possible, to help the computation speed. There should be NO serial loops if its possible to replace them. IMPORTANT: you must check your work when you implement multiprocessing and vectorization.

Functions you must NOT change:
- `load_lifetime_data`

Don't tackle this problem all at once; split it into parts to avoid hitting the OUTPUT TOKEN limit.

All the tools you should revise are under `tools/` -- revise nothing else.