Invoke the `experiment-background` skill before proceeding. Then read PLAN.md in full. Then read the experimental data files to understand their structure. Your goal is to build the Python tools that a scientist would need to carry out every step of the plan.

---

## What to produce

Write one Python file per main phase of the plan, plus one shared utilities file. Name each file to clearly reflect the kind of logic its useful for (e.g. physics modeling, statistics, etc.). Decide yourself what Python tools and functions will be required for doing each step. Each tool should have a clear use from the plan.

Each file must begin with a one-sentence header comment identifying the what logic it is useful for. Each function should also have a comment for the same purpose. Beyond that, add comments only where the logic is not self-evident from the code itself.

---

## Code standards

- **Runnable**: Before finishing, execute each file and fix any errors. Every file must import cleanly and its `if __name__ == "__main__"` block must complete without exceptions, demonstrating that the tools work on real or representative data.
- **Readable, not verbose**: Prefer clear variable names and structure over inline comments. Do not pad short functions with unnecessary commentary.
- **Dependencies**: Before writing any import, confirm the package is installed by running `python -c "import <package>"`. If a package is missing, use an available alternative or implement the functionality directly.
- **Avoid hard-coded values**: Hard-coded numbers are subject to hallucination. Use programmatic values whenever possible.
- **No global state**: Functions take explicit arguments and return explicit values.
- **Usage**: An agent should be able to use the tools like an importable package.

IMPORTANT: Check if there are any FIT NOTES in the experiment-background skill and be sure the tools implement these notes.

---

## Verification

After writing all files, run each one and confirm it exits without errors. Report the output of each run in your final message.

## Note: You may edit functions in data_io.py to fit your needs but DO NOT edit `load_lifetime_data`.